class UserData{
    String uid;
    String fullName;
    UserData({this.uid, this.fullName});
}
class UserDataAdmin{
    String uid;
    String username;
    String password;
    UserDataAdmin({this.uid, this.username, this.password});
}
